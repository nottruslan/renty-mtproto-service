# Быстрый старт MTProto сервиса

## Шаг 1: Перейти в папку сервиса

```bash
cd mtproto-service
```

## Шаг 2: Запустить скрипт авторизации

```bash
node auth.js
```

**Что произойдет:**
- Скрипт подключится к Telegram
- Отправит код на номер +79920792251
- Попросит ввести код из Telegram

## Шаг 3: Ввести код из Telegram

Когда увидите сообщение:
```
📝 Введите код из Telegram:
```

Введите код, который придет в Telegram на номер +79920792251, и нажмите Enter.

**Если появится запрос пароля 2FA:**
```
🔒 Требуется пароль двухфакторной аутентификации (2FA)
📝 Введите пароль 2FA:
```

Введите пароль двухфакторной аутентификации (который вы установили в настройках Telegram).

## Шаг 4: Скопировать SESSION_STRING

После успешной авторизации вы увидите что-то вроде:
```
💾 Сохраните эту сессию в .env файл:
TELEGRAM_MANAGER_SESSION_STRING=1BVtsOMwBu4...
```

Скопируйте всю строку после `TELEGRAM_MANAGER_SESSION_STRING=`

## Шаг 5: Добавить SESSION_STRING в .env файл

Откройте файл `.env` в папке `mtproto-service` и добавьте/обновите строку:

```env
TELEGRAM_MANAGER_SESSION_STRING=ваша_скопированная_сессия
```

## Шаг 6: Запустить сервис

```bash
npm start
```

Сервис запустится на `http://localhost:3000`

## Проверка работы

В другом терминале проверьте:

```bash
curl http://localhost:3000/health
```

Должен вернуться: `{"status":"ok","clientReady":true}`

## Готово!

Теперь сервис готов создавать групповые чаты. Убедитесь, что:
1. Сервис запущен (`npm start`)
2. В Supabase Secrets добавлен `MTPROTO_SERVICE_URL=http://localhost:3000` (или URL вашего деплоя)

